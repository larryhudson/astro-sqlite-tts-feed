---
import { checkLoggedIn } from "@src/utils/auth";
const isLoggedIn = checkLoggedIn(Astro);
if (!isLoggedIn) {
  return Astro.redirect("/auth/login");
} else {
  return Astro.redirect("/articles/");
}
---
